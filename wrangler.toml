# Nur statische Webseite — Cloudflare Pages (NICHT Worker)
# Pages-Deploy: npx wrangler pages deploy docs --project-name=gifthismilk
# Worker-Deploy (wrangler deploy) bitte NICHT für dieses Projekt nutzen.
